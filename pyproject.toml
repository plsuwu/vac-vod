[project]
name = "embedding"
version = "0.1.0"
readme = "README.md"
authors = [
    { name = "plsuwu", email = "124419933+plsuwu@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
embedding = "embedding:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
